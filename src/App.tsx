import React from 'react';
import './App.css';
import { Button } from '@mui/material';

function App() {
  return (
    <div>
      <h1>Mix and Magic</h1>
        <Button variant='contained'>Cake Configurator</Button>
        <Button variant='contained'>Personal Projects</Button>
        <Button variant='contained'>Flavor Finder</Button>
        <Button variant='contained'>Refined Recipes</Button>
    </div>
  );
}

export default App;
